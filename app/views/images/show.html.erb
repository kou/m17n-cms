<h1><%=h @image.filename %></h1>

<p>
  <%= link_to_function(image_tag(image_path(@image, :format => :png)),
                       "select_image(#{image_path(@image, :format => :png).to_json})") %>
</p>

<p>(<%= @image.width %>x<%= @image.height %>)</p>

<%= link_to(t('Index'), images_path) %>
